## What changed and why

<!-- Lead with the point. What does this change, and what problem does it solve? -->

## Checklist

- [ ] `node bin/biswodip.mjs verify-package --verbose` passes
- [ ] `node --test test/` passes
- [ ] `skills/` regenerated with `node bin/biswodip.mjs build-skill` (if sources changed)
- [ ] No file under `upstream/` was edited by hand
- [ ] SPDX header present on any new `.mjs`, `.sh` or `.ps1`
- [ ] No secrets, keys or personal data added
- [ ] Documentation matches what the code actually does
- [ ] Prose run through the `no-ai-slop` skill

## Evidence

<!-- Commands run and their exit codes. "It works" is not evidence. -->
